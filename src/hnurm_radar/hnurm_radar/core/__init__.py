"""
core — 核心抽象层

提供检测器、跟踪器、传感器的基类接口定义。
各方案节点（camera_scheme、lidar_scheme、air_scheme）继承这些基类，
实现具体的算法逻辑，统一编程范式，便于测试和替换。

设计原则：
  1. 基类只定义接口契约，不引入 ROS2 或硬件依赖
  2. 具体实现通过构造函数注入依赖（依赖注入）
  3. 所有可调参数通过 configs/ 配置文件加载
"""
